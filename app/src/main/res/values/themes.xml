<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Bare-minimum Material3 base theme. The actual color scheme is produced
         in Compose (Theme.kt); this exists so the activity/splash has something
         to latch onto before Compose takes over. -->
    <style name="Theme.MediaPlayer" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
